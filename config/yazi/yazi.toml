[mgr]
show_hidden = true

[opener]
edit = [
	{ run = "nvim %s", block = true, for = "unix" },
]

[open]
rules = [
	{ mime = "text/*", use = "edit" },
	{ mime = "application/json", use = "edit" },
	{ mime = "application/x-yaml", use = "edit" },
]
